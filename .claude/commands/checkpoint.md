Stage all changed files. Show me a git diff summary of what changed (files modified, files added, files deleted).

Then write a commit message in this format:

feat(<feature>): <short summary>

- <bullet point of what was built>
- <bullet point of what was built>
- <schema changes if any>
- <key decisions made>

Ask me to confirm the message before committing.
If I confirm, commit and push to origin main.
If I ask for changes, revise the message and ask again.
